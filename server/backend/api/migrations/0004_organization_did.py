from django.db import migrations


class Migration(migrations.Migration):
	dependencies = [
		('api', '0003_worker_fields'),
	]

	operations = [
		# This migration is intentionally left empty to fix a prior broken file.
		# Subsequent migrations will introduce new models and changes.
	]

